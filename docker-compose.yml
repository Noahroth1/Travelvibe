services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: travelvibe
      POSTGRES_PASSWORD: travelvibe
      POSTGRES_DB: travelvibe
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

# Copy to `.env` in this folder. Used by FastAPI via pydantic-settings.

# After `docker compose up -d` with the root docker-compose.yml:
# DATABASE_URL=postgresql+asyncpg://travelvibe:travelvibe@localhost:5432/travelvibe

DATABASE_URL=
